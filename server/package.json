{
  "name": "tradez-electric",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js",
    "server": "nodemon server.js",
    "client": "cd ../ && cd client && npm start && cd -",
    "heroku-postbuild": "cd client && npm install --only=dev && npm install && npm run build"    "dev": "concurrently \"npm run server\" \"npm run client\""
  },
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "nodemon": "^2.0.3"
  },
  "devDependencies": {
    "concurrently": "^4.1.2"
  }
}
